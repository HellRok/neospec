#!/usr/bin/env bash
set -euo pipefail

.buildkite/bin/install-asdf
echo $PATH

source /root/.bashrc

asdf install ruby
gem install bundler
asdf reshim

ls -l /root/.asdf/shims
echo $PATH
bundle install
